# 潔牙記錄系統 V1.1 規格

最後整理日期：2026-09-13

本文件已整合下列確認決策：

* 學生不需要個人帳號；教室固定裝置使用專用 Supabase Auth 帳號。
* 教師使用另一個 Supabase Auth 帳號登入月報與設定頁。
* 教師可維護報表行政資料及特殊日期，但不可修改潔牙紀錄。
* 教師可在設定頁選擇「週一至週五」或「星期一至星期日」為一般使用日；特殊日期設定具有最高優先權。
* 已明確定義零分母、當月未來日期及非上課日的統計規則。
* 教師可按學期設定兩位學生的顯示姓名與選填圖片；學生頁顯示姓名與圖片，月報顯示姓名。

## 1. 專案概述

建立一套供台灣國小教室使用的「學生自主潔牙記錄系統」。

學生完成潔牙後，自行走到教室固定式觸控螢幕，點選自己的座號，即完成當日潔牙紀錄。

本系統不是傳統行政管理系統。

產品核心是：

> 學生自主操作、一次觸控完成、兩位學生、大型觸控介面、潔牙健康教育主題。

V1.1 優先確保每日實際使用簡單、穩定、清楚。

---

# 2. 技術架構

採用：

* Frontend：HTML5 / CSS3 / Vanilla JavaScript
* Hosting：GitHub Pages
* Backend / Database：Supabase
* Database：PostgreSQL
* Authentication：Supabase Auth
* 時區：Asia/Taipei
* 主要使用裝置：固定式橫向觸控螢幕
* 主要比例：16:9
* 報表：A4 橫式列印

V1.1 不使用 React、Vue、Angular 等前端框架。

除 Supabase 官方 JavaScript Client 外，盡量不增加第三方 dependency。

---

# 3. V1.1 使用者與帳號

目前班級只有兩位學生：

* 1號
* 2號

V1.1 直接固定兩個學生座號，姓名與圖片只能由教師按學期設定，不提供新增或移除座號。

不需要：

* 學生個人帳號
* 學生密碼
* 學生自行修改姓名或圖片
* 班級選擇
* 動態學生管理
* 家長登入

未來若增加轉學生，再修改。

系統另有兩種非學生帳號：

## 3.1 教室裝置帳號

* 綁定教室固定式觸控螢幕使用。
* 由老師在首次設定或登入狀態失效時登入。
* 白板端使用簡單登入名稱 `user` 與密碼，不要求老師在白板輸入 Email。
* Supabase Auth 底層仍使用專用 Email／Password 使用者；前端將 `user` 對應至公開設定的內部 Auth Email。
* 內部 Auth Email 與登入名稱不是安全邊界；密碼不得寫入前端、SQL seed 或 repository。
* 學生不會看到帳號密碼，也不需要自行登入。
* 只能讀取當日狀態、讀取當日行事曆設定，以及新增當日 `completed` 或 `leave` 紀錄。
* 不可讀取歷史月報、修改既有潔牙紀錄或管理系統設定。

## 3.2 教師帳號

* 用於登入 `report.html` 與 `settings.html`。
* 可讀取歷史潔牙紀錄及列印月報。
* 可管理報表行政資料與特殊日期。
* 不可新增、修改或刪除 `brushing_records`。

兩種帳號皆由管理者在 Supabase Auth 建立。kiosk 在白板呈現帳號式介面，teacher 使用 Email／Password。V1.1 不提供公開註冊、忘記密碼或前端帳號管理功能。

---

# 4. 學生端主要操作流程

前置條件：教室固定裝置已由老師使用 `kiosk` 帳號登入。若 session 失效，學生卡片必須停用並顯示需要老師重新登入的設定畫面，不可退回匿名資料存取。

學生完成潔牙：

1. 完成潔牙。
2. 走到觸控螢幕。
3. 找到自己的座號。
4. 點擊自己的大型學生卡片。
5. 系統寫入今日潔牙紀錄。
6. 卡片立即顯示完成狀態。
7. 不跳頁。
8. 不需要再次確認。

正常潔牙操作必須做到：

> 一次觸控完成。

---

# 5. 學生端主畫面

主頁：

`index.html`

畫面採單頁設計。

主要區域：

1. 標題
2. 日期
3. 操作提示
4. 1號大型卡片（姓名、選填圖片、座號）
5. 2號大型卡片（姓名、選填圖片、座號）
6. 今日完成進度
7. 潔牙健康教育短句

概念：

```text
┌────────────────────────────────────────────────────────┐
│                                                        │
│ 🦷 今天潔牙了嗎？                     9月13日 星期日   │
│ 刷完牙，點一下自己的座號                              │
│                                                        │
│ ┌────────────────────┐ ┌────────────────────┐         │
│ │                    │ │                    │         │
│ │  [圖片] 小星 1號   │ │  [圖片] 小宇 2號   │         │
│ │                    │ │                    │         │
│ │       🦷🪥         │ │       🦷🪥         │         │
│ │                    │ │                    │         │
│ │   點一下完成潔牙   │ │   點一下完成潔牙   │         │
│ │                    │ │                    │         │
│ │     今日請假       │ │     今日請假       │         │
│ └────────────────────┘ └────────────────────┘         │
│                                                        │
│                  今日完成 0 / 2                        │
│              ━━━━━━━━━━━━━━━━━                         │
│                                                        │
│               刷乾淨，笑容亮晶晶 ✨                   │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

# 6. UI / UX 原則

## 6.1 觸控優先

* 整張學生主要卡片為大型觸控區。
* 不依賴 hover。
* 不要求精準點擊。
* 不設計小型主要操作按鈕。
* 重要資訊應能從一定距離辨識。
* 避免捲動。
* 16:9 畫面原則上應完整顯示所有主要內容。

## 6.2 視覺風格

定位：

> 兒童健康教育＋潔牙主題

不要設計成：

> 學校行政資訊系統

視覺元素可使用：

* 牙齒
* 牙刷
* 泡泡
* 閃亮星星
* 清潔元素

但裝飾不可妨礙操作辨識。

整體：

* 淺色背景
* 大量留白
* 大型圓角卡片
* 清楚層級
* 柔和配色
* 高可讀性

參考色彩：

```css
--background: #F4FBFC;
--card: #FFFFFF;
--primary: #55B8D0;
--text: #24444A;
--completed-bg: #E5F7E9;
--completed: #56B870;
```

實作時可依整體視覺稍微調整。

---

# 7. 學生每日狀態

每位學生每天有三種 UI 狀態：

## 7.1 未完成

顯示：

* 座號
* 牙齒／牙刷圖示
* 「點一下完成潔牙」
* 「今日請假」

主卡片為白色或淡藍色。

## 7.2 已完成潔牙

顯示：

* 座號
* ✓
* 牙齒
* 小星星
* 「今天完成！」
* 「好棒，繼續保持」

卡片改為淡綠色。

## 7.3 請假

顯示：

* 座號
* 適當的請假圖示
* 「今日請假」
* 「不列入潔牙統計」

使用淡橘色／淡黃色等與完成狀態容易區分的顏色。

---

# 8. 潔牙完成動畫

學生點擊主卡片後，提供約 0.6～0.9 秒的簡短回饋。

建議：

1. 卡片輕微縮小。
2. 回彈。
3. ✓ 出現。
4. 牙齒旁出現少量星星。
5. 卡片背景淡入完成色。

避免：

* 全螢幕彩帶
* 長時間動畫
* Modal
* 第二次確認
* 音效依賴
* 阻礙下一位學生操作的動畫

動畫完成後仍留在主畫面。

---

# 9. 重複潔牙點擊

同一學生同一天只能存在一筆有效狀態紀錄。

若已完成潔牙後再次點擊：

* 不新增第二筆紀錄。
* 不取消紀錄。
* 不改變完成時間。
* 可短暫顯示：

「今天已經完成囉！」

約 1～2 秒後自動消失。

資料庫必須以 unique constraint 防止重複資料。

---

# 10. 請假功能

每位學生卡片提供次要操作：

「今日請假」

由老師或另一位學生代為操作。

因請假屬於與潔牙不同的狀態，而且可能誤觸，因此點擊「今日請假」後需要二次確認：

```text
1號今天請假嗎？

[取消] [確認請假]
```

確認後：

* 寫入今日 `leave` 狀態。
* 卡片顯示「今日請假」。
* 不列入當日／當月潔牙完成率分母。

請假操作不可因誤觸主卡片而觸發。

---

# 11. 日期與跨日處理

所有日期邏輯以：

`Asia/Taipei`

為準。

不可直接依 UTC 日期判定「今天」。

每天不需要刪除或重設前一天資料。

主畫面只查詢「今天」的紀錄。

例如：

```text
9/13
1號 completed
2號 completed

9/14
查詢 9/14

若無資料：

1號 incomplete
2號 incomplete
```

9/13 紀錄永久保留。

如果頁面跨越午夜仍持續開啟，系統應能偵測日期改變並重新載入當日狀態，不要求人工重新整理。

---

# 12. 頁面重新整理

不可只依賴前端記憶體狀態。

每次：

* 開啟頁面
* Refresh
* 網路重新連線

都應重新向 Supabase 查詢當日紀錄。

例如 1號已完成後重新整理：

```text
1號：今天完成
2號：尚未完成
```

狀態不能消失。

---

# 13. 今日完成進度

主畫面底部顯示：

```text
今日完成 1 / 2
```

並搭配大型簡單進度條。

請假學生不視為「未完成」。

例如：

1號完成、2號請假：

可顯示：

```text
今日潔牙 1 / 1
2號請假
```

當所有當日應潔牙學生均完成時，顯示：

「今天大家都完成潔牙了！」

可提供非常簡短的完成視覺效果。

完成判定僅在分母大於 `0` 時成立。

零分母情境：

* 兩位學生皆請假時，顯示「今日學生皆請假，無需潔牙紀錄」。
* 當天是非上課日時，顯示假別；若無假別則顯示「今日非上課日」。
* 不顯示 `0 / 0`。
* 不顯示「今天大家都完成潔牙了！」，也不播放全員完成動畫。
* 進度條改為中性狀態，不計算百分比。

---

# 14. 教師頁面與登入

教師頁面包括：

* `report.html`：查看及列印月報。
* `settings.html`：維護報表行政資料、學生顯示資料、每週使用日與特殊日期。

兩個頁面共用 Supabase Auth 登入狀態。

未登入時：

* 只顯示教師帳號及密碼登入表單。
* 不查詢或顯示任何學生紀錄、月報或設定資料。

登入成功後必須確認帳號角色為 `teacher`。非教師角色不可進入上述頁面。

登入失敗統一顯示「帳號或密碼錯誤」，不得在畫面洩漏技術細節或帳號是否存在。

頁面必須提供登出功能。帳號及密碼不得寫入 HTML、JavaScript、SQL seed 或 Git repository。

## 14.1 月匯整報表

教師月報頁：

`report.html`

主要用途：

* 查看每月潔牙紀錄
* 學校行政留存
* A4 列印／另存 PDF

報表不是學生每日操作主畫面的一部分。

## 14.2 資料設定頁

`settings.html` 提供四個設定區：

1. 報表行政資料
2. 學生姓名與圖片
3. 每週使用日期
4. 特殊日期設定

學生姓名與圖片按 `report_settings` 的學期保存。姓名為必填、最長 30 字；圖片選填，只接受 JPEG、PNG、WebP 且不得超過 2 MB。圖片儲存在 Supabase 私人 Storage bucket，不得放進 Git repository。

學生使用的 `index.html` 不顯示管理頁入口。

---

# 15. 月報表格式

紙張：

* A4
* 橫式

資料方向：

* 日期 = X 軸
* 學生 = Y 軸，列標題顯示「座號 姓名」；沒有學生設定時只顯示座號

上方顯示：

* 學校名稱
* 學年度／學期
* 月份
* 班級
* 必要的行政欄位

概念：

```text
○○國民小學
115學年度第1學期 9月份學生潔牙紀錄表

             1   2   3   4   5   6   7 ... 28 29 30
星期         二  三  四  五  六  日  一     一 二 三
假別                         休  休
-------------------------------------------------------
1號          ✓   ✓   ✓   ✓   ／  ／  ✓ ... ✓ ✓ ✓
2號          ✓   ✓       ✓   ／  ／  ✓ ... ✓ ✓ ✓
```

---

# 16. 月表符號

正式列印版優先考慮黑白印表機。

使用：

* `✓` = 已完成潔牙
* `假` = 學生請假
* 空白 = 上課日但沒有完成潔牙紀錄
* `／` = 非上課日

避免依賴顏色才能理解資料。

---

# 17. 星期

月報表必須顯示星期。

例如：

```text
日期  1  2  3  4  5
星期  二 三 四 五 六
```

週六、週日可使用淡灰背景方便閱讀。

---

# 18. 國定假日與非上課日

月報表必須能辨識：

* 星期六
* 星期日
* 國定假日
* 補假
* 學校特殊停課日
* 補行上課日

一般規則由老師在 `settings.html` 選擇：

* `weekdays`：週一至週五為上課／可使用日，週六、週日為非上課日。
* `everyday`：星期一至星期日皆為上課／可使用日。
* 初始值為 `weekdays`，確保升級後維持原有行為。
* `school_calendar` 中的明確設定一律優先於每週使用模式。

因此，任何日期都可另設為特殊假日或上課／可使用日。

非上課日：

* 顯示 `／`
* 不列入應潔牙日數。
* 可在「假別」列顯示簡稱。

例如：

* 中秋
* 國慶
* 教師節
* 補假

---

# 19. 學校行事曆

建立獨立的學校行事曆資料結構。

用途：

判定某日期是否為應潔牙日。

至少包含：

```text
date
is_school_day
label
created_at
updated_at
updated_by
```

例如：

```text
2026-09-28
false
教師節補假
```

以及特殊補行上課：

```text
2026-10-03
true
補行上課
```

系統應以 `school_calendar` 的明確設定優先於 `app_settings.usage_day_mode`。

## 19.1 每週使用日期設定介面

老師登入 `settings.html` 後，可選擇：

* 週一至週五
* 星期一至星期日（七日皆可）

儲存後，學生頁、資料庫寫入檢查及月報統計必須使用相同設定。只有啟用中的 teacher 可以修改，kiosk 僅能讀取這一項執行所需設定。

## 19.2 特殊日期設定介面

老師登入 `settings.html` 後，可使用「特殊日期設定」區新增或修改：

* 日期
* 日期類型：非上課日或上課／可使用日
* 假別／說明，例如「國慶日」、「教師節補假」、「校慶補假」或「補行上課」

下方應顯示當月已設定的特殊日期，並提供編輯及刪除功能。

刪除前需要二次確認。刪除特殊設定後，該日期恢復使用每週使用模式。

同一日期再次儲存時更新原設定，不建立重複資料。

## 19.3 非上課日學生畫面

若今天依上述規則判定為非上課日：

* 學生卡片不得建立潔牙或請假紀錄。
* 顯示該日假別；若沒有標籤則顯示「今日非上課日」。
* 資料庫端必須同時拒絕非上課日的新增請求，不可只依賴前端停用按鈕。

---

# 20. 月統計

每位學生顯示：

* 本月應潔牙日數
* 完成日數
* 請假日數
* 完成率

計算：

```text
個人應潔牙日數
= 全月上課日
- 該生請假日數
```

完成率：

```text
完成日數 / 個人應潔牙日數 × 100%
```

例如：

```text
本月上課日：20
1號請假：2
1號應潔牙：18
1號完成：17

完成率：
17 / 18 = 94.4%
```

非上課日不得進入分母。

統計日期範圍：

* 過去月份計算完整月份。
* 當月只計算到台北日期的今天，尚未到來的上課日不提前進入分母。
* 未來月份的應潔牙日數為 `0`。

零分母規則：

* 個人應潔牙日數為 `0` 時，完成率顯示 `—`。
* 同時顯示「無應潔牙日」，不顯示 `0%` 或 `100%`。
* 完成率四捨五入到小數點後一位；整數結果不顯示多餘小數，例如 `100%`。
* 非上課日即使意外存在歷史紀錄，也不計入完成數或分母，月表仍顯示 `／`。

---

# 21. A4 列印

`report.html` 必須提供良好的 CSS Print Styles。

使用：

```css
@media print
```

以及：

```css
@page {
  size: A4 landscape;
}
```

目標：

一個月份、目前兩位學生原則上完整列印在一張 A4 橫式紙張。

列印時：

* 隱藏網頁操作按鈕
* 隱藏不必要裝飾
* 保留表格框線
* 黑白列印仍可辨識
* 避免表格超出紙張
* 避免日期欄被切割

V1.1 不需要自行生成 PDF。

使用瀏覽器：

「列印 → 另存為 PDF」

即可。

---

# 22. 資料庫設計原則

不要每個月建立新的 PostgreSQL Table。

所有每日紀錄存在同一資料表。

月報表由指定日期區間即時查詢產生。

核心 Table：

## brushing_records

至少包含：

```text
id
student_no
record_date
status
recorded_at
```

status：

```text
completed
leave
```

「未完成」不需要寫入資料庫。

沒有紀錄即表示當日尚未完成。

必須建立：

```text
UNIQUE(student_no, record_date)
```

確保同一學生同一天只能有一個有效狀態。

---

# 23. 支援資料表

## 23.1 school_calendar

建議：

```text
id
date
is_school_day
label
created_at
updated_at
updated_by
```

`date` 必須唯一。

用途：

* 國定假日
* 補假
* 校內停課
* 補行上課
* 其他特殊日期

`updated_by` 保存最後修改者的 Supabase Auth 使用者 ID。

## 23.2 app_settings

保存一筆全系統設定：

```text
id = 1
usage_day_mode = weekdays | everyday
created_at
updated_at
updated_by
```

`id` 固定為 `1`，`usage_day_mode` 預設為 `weekdays`。只有 teacher 可修改；kiosk 只能讀取判定今日是否可使用所需的這一筆設定。

## 23.3 report_settings

每學年度、每學期保存一筆報表行政設定，避免新學期的修改影響歷史報表。

至少包含：

```text
id
school_name
class_name
academic_year
semester
effective_start
effective_end
form_title
created_at
updated_at
updated_by
```

限制：

* `semester` 只能是 `1` 或 `2`。
* `effective_end` 不得早於 `effective_start`。
* 同一學年度及學期只能有一筆設定。
* 不同設定的有效日期區間不得互相重疊。
* `updated_by` 保存最後修改者的 Supabase Auth 使用者 ID。

老師可在 `settings.html` 新增下一學期設定，或編輯既有設定。

月報以所選月份第一天查找適用的有效日期區間。若找不到適用設定，顯示「尚未設定此月份的報表資料」，不可自行猜測學年度、學期或班級。

設定欄位：

* 學校名稱
* 班級
* 學年度
* 學期
* 學期開始日期
* 學期結束日期
* 表單標題，預設「學生潔牙紀錄表」

## 23.4 student_profiles

按 `report_settings` 學期保存兩個固定座號的顯示資料，避免改名後影響歷史月報。

至少包含：

```text
id
report_setting_id
student_no
display_name
photo_path
created_at
updated_at
updated_by
```

限制：

* `student_no` 只能是 `1` 或 `2`。
* 同一個 `report_setting_id` 與 `student_no` 只能有一筆。
* `display_name` 去除前後空白後必須為 1 至 30 字。
* `photo_path` 只保存私人 Storage 物件路徑，不保存公開 URL。
* 圖片 bucket 名稱為 `student-photos`，必須設為 private，檔案上限 2 MB，只接受 JPEG、PNG、WebP。
* 不在 SQL seed 或 repository 放入實際姓名與圖片。

## 23.5 app_users

保存 Supabase Auth 使用者與應用程式角色的對應，不保存密碼。

至少包含：

```text
user_id
role
display_name
is_active
created_at
updated_at
```

`role` 只能是：

* `kiosk`
* `teacher`

帳號密碼由 Supabase Auth 的 `auth.users` 安全管理，密碼不得存入 `app_users` 或任何 public schema 資料表。

---

# 24. 狀態修改原則

V1.1 學生端不得透過再次點擊直接取消紀錄。

例如：

```text
completed
```

不能再點一下變成：

```text
incomplete
```

避免觸控誤操作。

若日後需要：

* 更正誤登
* 補登
* 修改請假

應由教師管理功能處理。

V1.1 可暫不提供完整教師修改後台。

V1.1 採「第一筆有效紀錄鎖定」：

* 已完成潔牙後，不能再改成請假。
* 已請假後，不能再改成完成潔牙。
* 主畫面遇到既有紀錄時，重新查詢並顯示目前狀態。
* 若確實需要更正，僅由具備 Supabase 管理權限的人員在 Dashboard 處理；管理密鑰不得提供給前端。

---

# 25. Supabase Security

Frontend 只能使用適合公開前端使用的 Supabase public / anon key。

絕對不可把以下內容放進：

* GitHub Repository
* GitHub Pages
* JavaScript
* HTML

包括：

* Supabase service_role key
* Database password
* 任何管理權限 secret

必須設定 PostgreSQL / Supabase Row Level Security（RLS）。

V1.1 採用 Supabase Auth，不提供未登入的公開資料存取。公開／anon key 可以出現在靜態前端，因為它不是管理密鑰；實際資料權限必須由登入 session、角色與 RLS 共同限制。

必須關閉公開註冊。`kiosk` 與 `teacher` 帳號由管理者在 Supabase Dashboard 建立，再於 `app_users` 指定角色。kiosk 可在前端以固定登入名稱呈現，但底層仍須使用獨立的 Supabase Auth 使用者，不得改成前端硬編碼密碼或匿名寫入。

權限矩陣：

| 操作 | 未登入 anon | kiosk | teacher |
| --- | --- | --- | --- |
| 讀取今日潔牙狀態 | 否 | 是 | 是 |
| 新增今日潔牙／請假 | 否 | 是 | 否 |
| 修改／刪除潔牙紀錄 | 否 | 否 | 否 |
| 讀取歷史月報 | 否 | 否 | 是 |
| 讀取每週使用模式 | 否 | 是 | 是 |
| 管理每週使用模式 | 否 | 否 | 是 |
| 讀取報表行政設定 | 否 | 否 | 是 |
| 管理報表行政設定 | 否 | 否 | 是 |
| 讀取學生顯示資料 | 否 | 僅目前學期 | 是 |
| 管理學生姓名／圖片 | 否 | 否 | 是 |
| 讀取學生私人圖片 | 否 | 僅目前學期引用圖片 | 是 |
| 管理特殊日期 | 否 | 否 | 是 |

`kiosk` 新增紀錄時，RLS 必須確認：

* 使用者角色為啟用中的 `kiosk`。
* `student_no` 只能是 `1` 或 `2`。
* `status` 只能是 `completed` 或 `leave`。
* `record_date` 必須是資料庫依 `Asia/Taipei` 判定的今天。
* 今天必須是上課／可使用日；`school_calendar` 明確設定優先，否則套用資料庫中的每週使用模式。

教師管理 `app_settings`、`school_calendar`、`report_settings`、`student_profiles` 與學生圖片時，RLS 必須確認角色為啟用中的 `teacher`，並由資料庫記錄 `updated_by`。Storage bucket 必須保持 private；kiosk 只能讀取目前台北日期所屬學期、且確實被 `student_profiles.photo_path` 引用的物件。

Codex 必須：

1. 提供完整 database schema SQL。
2. 提供 constraints。
3. 提供必要 indexes。
4. 提供 RLS 設定。
5. 說明 anon、kiosk 與 teacher 實際擁有哪些權限。
6. 不得為方便而直接關閉 RLS。

若某項操作無法在 GitHub Pages 與 Supabase Auth／RLS 架構下安全提供，應明確指出，不可透過暴露管理 secret 解決。

---

# 26. 網路與錯誤處理

學生點擊後：

只有 Supabase 寫入成功，才可正式顯示完成狀態。

若網路或資料庫失敗：

* 不得假裝成功。
* 顯示簡單明確提示。

例如：

「記錄沒有成功，請再試一次。」

提供重新嘗試。

避免將技術錯誤訊息直接顯示給學生。

Console 可保留開發用詳細錯誤。

---

# 27. 防止快速重複點擊

點擊後等待 Supabase response 期間：

* 暫時鎖定該學生卡片。
* 顯示簡短 loading 狀態。
* 防止連續觸控送出多個 request。

資料庫 unique constraint 作為最後一道防線。

---

# 28. Accessibility / 易用性

至少做到：

* 足夠的文字／背景對比。
* 不只依賴顏色表示狀態。
* 大型觸控區。
* semantic HTML。
* 適當 ARIA label。
* `prefers-reduced-motion` 時減少動畫。
* 不需要 hover 才能取得資訊。
* 使用清楚的繁體中文。
* 採台灣用語。

---

# 29. Responsive Design

主要目標：

16:9 橫式觸控螢幕。

至少測試：

* 1920×1080
* 1366×768
* 1280×720

上述解析度原則上不應需要垂直捲動即可完成學生主要操作。

手機不是 V1.1 主要學生操作裝置，但頁面不可完全失效。

---

# 30. 專案結構

建議：

```text
/
├── index.html
├── report.html
├── settings.html
├── css/
│   ├── style.css
│   └── print.css
├── js/
│   ├── config.js
│   ├── supabase.js
│   ├── auth.js
│   ├── app.js
│   ├── report.js
│   └── settings.js
├── assets/
│   └── ...
├── supabase/
│   └── schema.sql
├── SPEC.md
└── README.md
```

可依實際需要合理調整，但保持簡單。

---

# 31. GitHub Pages

專案必須可以直接部署到 GitHub Pages。

不可依賴：

* Node.js Server
* PHP
* Python Server
* 自架 Backend Server

Frontend 應為純靜態網站。

Supabase 負責後端資料服務。

---

# 32. README.md

Codex 應建立 README，至少包含：

1. 專案說明
2. GitHub Pages 部署方式
3. Supabase Project 設定
4. Schema SQL 執行方式
5. Supabase URL / public key 設定位置
6. Supabase Auth 設定及關閉公開註冊
7. kiosk 與 teacher 帳號建立方式
8. app_users 角色設定方式
9. RLS 與權限矩陣說明
10. app_settings 每週使用模式設定方式
11. school_calendar 特殊日期管理方式
12. report_settings 報表行政資料設定方式
13. student_profiles 與私人 Storage 設定方式
14. 本機測試方式
15. 正式部署方式
16. 常見錯誤排除

不得要求把敏感 secret commit 至 GitHub。

---

# 33. V1.1 不做

目前不要加入：

* 學生帳號
* 學生密碼
* 家長登入
* 多班級
* 動態學生管理
* QR Code
* 排行榜
* 個人頁面
* 複雜後台
* 過多統計圖表
* Excel 匯出
* CSV 匯出
* 自動 PDF 生成
* 老師留言
* 多項生活習慣檢核

保持 V1.1 單純。

---

# 34. 未來可能擴充

V1.2 或之後：

* 教師管理頁面
* 補登紀錄
* 更正錯誤
* 新增／移除學生
* 每週潔牙率
* 每月潔牙率
* 學期統計
* 月曆式紀錄
* CSV 匯出
* Excel 匯出
* 多班級
* 其他生活習慣紀錄

資料庫設計應避免阻礙這些合理擴充，但不要為未來需求過度工程化。

---

# 35. 隱私原則

學生端 V1.1：

* 顯示座號、教師設定的顯示姓名與選填圖片。
* 沒有設定時以「1號同學／2號同學」及座號圖示安全降級。
* 不蒐集不必要個人資料。

GitHub Repository 不得包含學生姓名或圖片；圖片只能進入受 Auth 與 RLS 保護的私人 Supabase Storage。

---

# 36. 驗收情境

完成 V1.1 後至少測試以下情境。

## Case 1：正常完成

初始：

```text
1號 未完成
2號 未完成
```

1號點擊：

```text
1號 已完成
2號 未完成
今日完成 1 / 2
```

重新整理：

狀態仍相同。

---

## Case 2：兩人完成

1號、2號皆完成：

```text
今日完成 2 / 2
```

顯示：

「今天大家都完成潔牙了！」

---

## Case 3：重複點擊

1號已完成後再次點擊：

* 不新增資料。
* 不取消。
* 顯示「今天已經完成囉！」

---

## Case 4：請假

2號選擇請假並確認：

```text
1號 未完成
2號 今日請假
```

2號不列入當日潔牙分母。

若1號完成：

```text
今日潔牙 1 / 1
```

---

## Case 5：重新整理

任一紀錄完成後重新整理頁面。

畫面必須由 Supabase 恢復正確狀態。

---

## Case 6：跨日

9/13 兩人完成。

9/14：

兩張卡片重新顯示未完成。

9/13 資料仍存在資料庫。

---

## Case 7：月報表

指定 2026 年 9 月。

系統正確顯示：

* 1～30日
* 星期
* 1號
* 2號
* 完成
* 未完成
* 請假
* 非上課日
* 完成日數
* 應潔牙日數
* 完成率

---

## Case 8：國定假日

若 school_calendar 指定：

```text
is_school_day = false
```

月表顯示非上課日。

不得計入應潔牙日數。

---

## Case 9：補行上課

即使日期是星期六，只要 school_calendar 指定：

```text
is_school_day = true
```

就必須視為應潔牙日。

另驗證每週使用模式：選擇 `everyday` 時，沒有特殊日期覆寫的星期六、星期日皆可登記並列入月報；切回 `weekdays` 後，週末恢復為非上課日。兩種模式下，`school_calendar` 覆寫都必須優先。

---

## Case 10：列印

使用 Chrome / Edge：

```text
列印
→ A4
→ 橫向
```

月表完整、清楚、無網頁操作按鈕，且黑白列印可辨識。

---

## Case 11：網路失敗

模擬 Supabase 無法連線。

學生點擊：

* 不得錯誤顯示「今天完成」。
* 顯示友善錯誤訊息。
* 可以重新嘗試。

---

## Case 12：教師登入保護

未登入時開啟 `report.html` 或 `settings.html`：

* 只顯示登入表單。
* 不載入任何學生紀錄或設定資料。

使用有效 `teacher` 帳號登入後，才可進入月報及設定頁。使用 `kiosk` 帳號不得進入。

---

## Case 13：特殊假日設定

老師在 `settings.html` 將一個平日設為非上課日並填寫假別：

* 儲存後可重新載入並看到相同設定。
* 月報顯示 `／` 與假別。
* 該日不進入分母。
* 該日學生端不可新增潔牙或請假紀錄。

刪除設定後，該日期恢復每週使用模式。

---

## Case 14：報表行政資料

老師新增一筆學年度及學期設定後：

* `report.html` 依所選月份載入正確的學校名稱、班級、學年度、學期及表單標題。
* 重新整理後資料仍存在。
* 找不到適用設定時顯示明確提示，不自行猜測資料。

---

## Case 15：零分母

兩位學生當日皆請假時：

* 不顯示 `0 / 0`。
* 顯示「今日學生皆請假，無需潔牙紀錄」。
* 不顯示全員完成祝賀。

學生當月應潔牙日數為 `0` 時，完成率顯示 `—` 與「無應潔牙日」。

---

## Case 16：資料庫權限

驗證：

* anon 無法讀寫任何業務資料。
* kiosk 只能讀取及新增台北日期今天的有效紀錄。
* kiosk 無法讀取歷史月報或管理設定，只能額外讀取判定今日可否使用所需的每週使用模式，以及目前學期的兩位學生顯示資料與被引用圖片。
* teacher 可讀取月報並管理 `app_settings`、`school_calendar`、`report_settings`、`student_profiles` 與學生圖片。
* teacher 無法新增、修改或刪除 `brushing_records`。

---

## Case 17：學生姓名

老師在 `settings.html` 選擇學期並設定兩位學生姓名後：

* `index.html` 顯示姓名與座號。
* `report.html` 的學生列標題顯示座號與該月份適用學期的姓名。
* 新學期改名不影響舊月份報表。
* 尚未設定學生資料時，學生頁退回「1號同學／2號同學」，報表只顯示座號。

---

## Case 18：學生圖片

老師上傳 JPEG、PNG 或 WebP 圖片後：

* 2 MB 以內可儲存並於學生頁顯示。
* 不支援的格式或超過 2 MB 時在上傳前顯示錯誤。
* 替換或移除後，重新整理仍維持正確狀態。
* 月報不顯示圖片。
* bucket 維持 private；anon、非目前學期的 kiosk 皆無法讀取圖片。

---

# 37. Codex 實作原則

請 Codex：

1. 先閱讀完整 `SPEC.md`。
2. 在寫程式前簡短列出 implementation plan。
3. 若 SPEC 有技術矛盾，先指出。
4. 不任意增加 V1.1 功能。
5. 優先保持架構簡單。
6. 使用 Vanilla HTML / CSS / JavaScript。
7. 建立完整 Supabase schema SQL。
8. 正確設定 RLS。
9. 建立 README。
10. 完成後依第 36 節逐項驗證。
11. 不將任何 secret 寫入 repository。
12. 若需要使用者在 Supabase Dashboard 或 GitHub 手動操作，請提供逐步指示。

---

# 38. V1.1 成功標準

本系統 V1.1 的成功，不以功能數量衡量。

成功標準是：

> 學生刷完牙後，走到教室觸控螢幕，找到自己的座號，只需要點一下，就能可靠完成今日潔牙紀錄。

同時：

> 老師月底能直接開啟月匯整表，以 A4 橫式列印出日期為 X 軸、學生為 Y 軸，包含星期、假日、請假及潔牙完成狀態的正式紀錄表。

只要這兩件事情簡單、穩定、清楚，就是 V1.1 的核心完成標準。
